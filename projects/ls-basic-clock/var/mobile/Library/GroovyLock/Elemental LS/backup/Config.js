var twentyfourhourtime = false; // true - use 24 hour time ; false - use 12 hour time
var showBackground = true; // true - show background ; false - don't show background
var showShadow = true; // true - show drop shadow; false - don't show drop shadow
var shadow = "6px 6px 6px 0px #888888"; // "horizontal-shadow-px | vertical-shadow-px | blur-dist-px | shadow-size-px | color-hex"
var timeColor = "#606060"; // Hex, rgb, or rgba can be used
var bgColor = "rgba(240, 240, 240, 0.5)"; // alpha controls opacity which holds a value between 0 and 1
var clockPosFromTop = 240; // Unit: Pixels
var boxWidth = 140; // Unit: Pixels
var boxHeight = 64; // Unit: Pixels
var textAlign = "center"; // Options: center, left, right
var fontSize = 30; // Unit: Pixels
var letterSpacing = 2; // Unit: Pixels

var twentyfourhourtime = false; // true - use 24 hour time ; false - use 12 hour time
var showBox = true; // true - show box behind text ; false - don't show box behind text
var showShadow = false; // true - show drop shadow; false - don't show drop shadow
var shadow = "6px 6px 6px 0px #202020"; // "horizontal-shadow-px | vertical-shadow-px | blur-dist-px | shadow-size-px | color-hex"
var timeColor = "#404040"; // Hex, rgb, or rgba can be used
var boxColor = "rgba(240, 240, 240, 0.5)"; // Options: Hex, rbg, or rbga (a = Alpha = opacity [value between 0 and 1])
var clockPosFromTop = 240; // Unit: Pixels - Distance from the top of the screen
var boxWidth = 140; // Unit: Pixels - Width of the box behind text
var boxHeight = 64; // Unit: Pixels - Height of the box behind text
var textAlign = "center"; // Options: center, left, right
var fontSize = 30; // Unit: Pixels - Size of the font
var letterSpacing = 2; // Unit: Pixels - Distance between characters

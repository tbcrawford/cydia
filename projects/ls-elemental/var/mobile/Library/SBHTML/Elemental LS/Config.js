var twentyfourhourtime = false; // true - use 24 hour time ; false - use 12 hour time
var showBox = true; // true - show box behind text ; false - don't show box behind text
var showShadow = true; // true - show drop shadow; false - don't show drop shadow
var zeroUnderTen = false; // true - adds 0 before hours under 10 ; false - does not add 0 before hours under 10
var shadow = "0 4px 6px 0 #D0D0D0"; // "horizontal-shadow-px | vertical-shadow-px | blur-dist-px | shadow-size-px | color-hex | inset (inner shadow instead of outer)"
var timeColor = "#404040"; // Hex, rgb, or rgba can be used
var boxColor = "rgba(240, 240, 240, 0.5)"; // Options: Hex, rbg, or rbga (a = Alpha = opacity [value between 0 and 1])
var clockPositionFromTop = "60px"; // Unit: Pixels - Distance from the top of the screen
var boxWidth = 140; // Unit: Pixels (px) - Width of the box behind text
var textAlign = "center"; // Options: center, left, right
var fontSize = "30px"; // Unit: Pixels - Size of the font
var letterSpacing = "2px"; // Unit: px or % - Distance between characters
var cornerRadius = "0px"; // Units: px or % - The radius of the corners
var borderStroke = "0px solid #404040"; // "border-thickness | Options: [solid, dotted, dashed, double, groove, ridge, inset, outset] | color"
